---
import Layout from '../layouts/Layout.astro';
import { getDatabaseContents, getDatabaseMetadata, mapPageProperties } from "../lib/notion";
import type { NotionPage } from "../types/notion";

// 0. 데이터베이스 메타데이터 및 태그 목록 추출
const database = await getDatabaseMetadata();
const dbTitle = database?.title?.[0]?.plain_text || "Blog Archive";
const tags = database?.properties?.Tags?.multi_select?.options?.map((opt: any) => opt.name) || [];

// 1. 게시글 데이터 가져오기
const allPagesRaw = await getDatabaseContents();
const posts = allPagesRaw
  .map(mapPageProperties)
  .filter((p: NotionPage) => p.slug && p.slug !== 'home'); // home 페이지는 목록에서 제외
---

<Layout title={dbTitle}>
  <main className="max-w-6xl mx-auto px-6 py-12 md:py-20">
    
    <div className="text-center mb-20 space-y-4">
      <h1 className="text-5xl md:text-7xl font-black text-slate-900 tracking-tight">
        Insight 
        <span className="text-primary">Archive</span>
      </h1>
      <p className="text-xl text-slate-500 max-w-2xl mx-auto font-medium">
        {database.description?.[0]?.plain_text || "Notion에서 작성하고 자동으로 발행되는 블로그입니다."}
      </p>

      {/* 태그 필터 (UI 준비) */}
      {tags.length > 0 && (
        <div className="flex flex-wrap justify-center gap-2 mt-8">
          <button className="px-4 py-2 rounded-full bg-primary text-white text-sm font-bold">All</button>
          {tags.map((tag: string) => (
            <button className="px-4 py-2 rounded-full bg-slate-100 text-slate-600 text-sm font-medium hover:bg-slate-200 transition-colors">
              {tag}
            </button>
          ))}
        </div>
      )}
    </div>

    <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      {posts.map((post: NotionPage) => (
        <article className="group bg-white rounded-3xl overflow-hidden border border-slate-100 hover:shadow-xl hover:-translate-y-1 transition-all duration-300 flex flex-col h-full">
          
          <a href={`/post/${post.slug}`} className="block overflow-hidden aspect-[1.5] relative bg-slate-50">
             {post.cover ? (
                <img src={post.cover} alt={post.title} className="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500" />
             ) : (
                <div className="w-full h-full flex items-center justify-center group-hover:scale-105 transition-transform duration-500">
                    <span className="text-4xl">📝</span>
                </div>
             )}
          </a>

          <div className="p-6 flex flex-col flex-1">
            <div className="flex items-center gap-3 mb-3">
               <span className="text-xs font-bold text-primary bg-slate-50 px-2 py-1 rounded">
                 {new Date(post.publishedAt).toLocaleDateString()}
               </span>
               {post.tags?.slice(0, 2).map((tag: string) => (
                 <span className="text-xs text-slate-400">#{tag}</span>
               ))}
            </div>
            
            <h2 className="text-xl font-bold text-slate-900 mb-3 leading-snug group-hover:text-primary transition-colors">
              <a href={`/post/${post.slug}`}>
                {post.title}
              </a>
            </h2>

            <p className="text-slate-500 text-sm line-clamp-2 mb-4 flex-1">
              클릭하여 내용을 확인하세요.
            </p>
            
            <a href={`/post/${post.slug}`} className="inline-flex items-center text-sm font-bold text-slate-900 hover:text-primary transition-colors mt-auto">
              Read Article 
              <svg xmlns="http://www.w3.org/2000/svg" className="h-4 w-4 ml-1" viewBox="0 0 20 20" fill="currentColor"><path fill-rule="evenodd" d="M12.293 5.293a1 1 0 011.414 0l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-2.293-2.293a1 1 0 010-1.414z" clip-rule="evenodd" /></svg>
            </a>
          </div>
        </article>
      ))}
    </div>

  </main>
</Layout>
