---
import NotionBlock from '../NotionBlock.astro';

const { block } = Astro.props;
---

<div class="flex flex-col md:flex-row gap-[46px] my-[1px] w-full">
  {block.children?.map((child: any) => (
    <NotionBlock block={child} />
  ))}
</div>
